{
  "scripts": {
  "dev": "netlify dev",
  "build": "npm --prefix backend run build && npm --prefix frontend run build"
},

  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "netlify": "^13.3.4"
  }
}
